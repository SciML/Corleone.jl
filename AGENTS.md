# Repository instructions

- When bumping the version of the root package or a sublibrary, update its matching compat entries in test group environments and dependent sibling sublibrary projects in the same change. Run the affected root and sublibrary test groups to verify that each checked-out package resolves at its new version.
